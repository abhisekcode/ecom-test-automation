[pytest]
addopts = --strict-markers -ra

markers =
    smoke: critical smoke tests
    regression: full regression suite
    ui: browser-driven UI tests
    api: HTTP API tests
    db: SQLite persistence tests
